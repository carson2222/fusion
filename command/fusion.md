---
description: Fan a hard task out to a model panel, then aggregate one answer
agent: build
---
Call the `fusion` tool with the exact task below, then return only its tool result verbatim.

$ARGUMENTS
